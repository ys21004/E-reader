'use strict';

/**
 * user-book-progress router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::user-book-progress.user-book-progress');
